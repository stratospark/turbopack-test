/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        turbo: {
            // resolveAlias: {
            //     'fs': 'empty',
            // },
        },
        }
};

export default nextConfig;
